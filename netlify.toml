[build]
  command = "bundle exec jekyll build"
  publish = "_site"

[build.environment]
  RUBY_VERSION = "3.2.0"
  JEKYLL_ENV = "production"

# Redirect rules for Jekyll
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
