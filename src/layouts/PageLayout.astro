---
import BaseHead from '../components/BaseHead.astro';
import type { ImageMetadata } from 'astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import FeedbackWidget from '../components/FeedbackWidget.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

interface Props {
  title?: string;
  description?: string;
  image?: ImageMetadata;
}

const {
  title = SITE_TITLE,
  description = SITE_DESCRIPTION,
  image
}: Props = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={title} description={description} image={image} />
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-JXQ5S5TPL7"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-JXQ5S5TPL7');
    </script>
    <script type="text/javascript">
      {(function(){
        const c=window; const l=document; const a="clarity"; const r="script"; const i="tuzlnmq3qr";
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        const t=l.createElement(r); t.async=1; t.src="https://www.clarity.ms/tag/"+i;
        const y=l.getElementsByTagName(r)[0]; y.parentNode.insertBefore(t,y);
      })()}
    </script>
  </head>
  <body class="bg-white text-gray-900">
    <Header />
    <main>
      <section class="mx-auto w-full max-w-4xl px-6 sm:px-8">
        <slot />
      </section>
    </main>
    <Footer />
    <FeedbackWidget />
  </body>
</html>
