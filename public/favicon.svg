<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 140 140" role="img" aria-labelledby="title desc">
  <title id="title">Stay Unfinished — Emblem Logo</title>
  <desc id="desc">An incomplete circular ring with a small square suggesting a cursor, symbolizing ongoing work.</desc>
  <defs>
    <mask id="notch">
      <rect x="0" y="0" width="140" height="140" fill="white"/>
      <!-- carve a notch at about 2 o'clock -->
      <path d="M110 38 L140 0 L140 60 Z" fill="black"/>
    </mask>
    <style>
      :root { color: #000; }
      @media (prefers-color-scheme: dark) {
        :root { color: #FFF; }
      }
    </style>
  </defs>
  <!-- ring -->
  <circle cx="70" cy="70" r="52" fill="none" stroke="currentColor" stroke-width="14" mask="url(#notch)"/>
  <!-- square "cursor" near the notch -->
  <rect x="102" y="26" width="14" height="14" rx="2" ry="2" fill="currentColor"/>
</svg>
